<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/stylesheets/output.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Asap&display=swap" rel="stylesheet">
    <title>Listagem de Prontuários - PediCare+</title>
</head>

<body class="bg-[#F0E8D9] font-sans">

    <!-- Navbar -->
    <header class="bg-[#2C5545] shadow-md p-4 flex items-center justify-between">
        <h1 class="text-white text-2xl font-bold">PediCare+ - Listagem de Prontuários</h1>
        <a href="/home/gerar_prontuario" class="bg-[#CAC2B5] hover:bg-[#938b7b] text-white font-semibold px-6 py-3 rounded-md">
            + Gerar Novo Prontuário
        </a>
    </header>

    <!-- Lista de Prontuários -->
    <main class="container mx-auto mt-6 p-6">
        <h2 class="text-xl font-bold text-[#2C5545] mb-4">Prontuários Cadastrados</h2>

        <% if (prontuarios.length === 0) { %>
            <!-- Mensagem caso não existam prontuários cadastrados -->
            <div class="text-center text-[#465C47] mt-8">
                <p class="text-lg font-semibold">Nenhum prontuário cadastrado no momento.</p>
                <p>Por favor, clique no botão acima para gerar um novo prontuário.</p>
            </div>
        <% } else { %>
            <!-- Tabela de Prontuários -->
            <div class="overflow-x-auto bg-white shadow-md rounded-lg">
                <table class="min-w-full text-left border-collapse">
                    <thead class="bg-[#E5E0D5] text-[#465C47]">
                        <tr>
                            <th class="py-3 px-4 text-center">Paciente</th>
                            <th class="py-3 px-4 text-center">Data de Criação</th>
                            <th class="py-3 px-4 text-center">Ações</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200">
                        <% prontuarios.forEach((prontuario) => { %>
                            <tr>
                                <td class="py-3 px-4 text-center"><%= prontuario.nome_paciente %></td>
                                <td class="py-3 px-4 text-center"><%= prontuario.data_criacao %></td>
                                <td class="py-3 px-4 flex justify-center gap-4">
                                    <a href="/prontuarios/visualizar/<%= prontuario.id_prontuario %>" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition">Visualizar</a>
                                    <form action="/prontuarios/<%= prontuario.id_prontuario %>?_method=DELETE" method="POST">
                                        <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition">Excluir</button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } %>
    </main>

</body>

</html>
