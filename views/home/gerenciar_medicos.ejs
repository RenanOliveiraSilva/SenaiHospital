<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/stylesheets/output.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Asap&display=swap" rel="stylesheet">
    <title>Listagem de Médicos - PediCare+</title>
</head>
<body class="bg-[#F0E8D9] font-sans">

    <!-- Navbar -->
    <header class="bg-[#2C5545] shadow-md p-4 flex items-center justify-between">
        <h1 class="text-white text-2xl font-bold">PediCare+ - Listagem de Médicos</h1>
        <a href="/home/cadastro_medico" class="bg-[#CAC2B5] hover:bg-[#938b7b] text-white font-semibold px-6 py-3 rounded-md">
            + Novo Médico
        </a>
    </header>

    <!-- Lista de Médicos -->
    <main class="container mx-auto mt-6 p-6">
        <h2 class="text-xl font-bold text-[#2C5545] mb-4">Médicos Cadastrados</h2>

        <% if (medicos.length === 0) { %>
            <!-- Mensagem caso não existam médicos cadastrados -->
            <div class="text-center text-[#465C47] mt-8">
                <p class="text-lg font-semibold">Nenhum médico cadastrado no momento.</p>
                <p>Por favor, clique no botão acima para adicionar um novo médico.</p>
            </div>
        <% } else { %>
            <!-- Tabela de Médicos -->
            <div class="overflow-x-auto bg-white shadow-md rounded-lg">
                <table class="min-w-full text-left border-collapse">
                    <thead class="bg-[#E5E0D5] text-[#465C47]">
                        <tr>
                            <th class="py-3 px-4 text-center">Nome</th>
                            <th class="py-3 px-4 text-center">Especialidade</th>
                            <th class="py-3 px-4 text-center">CRM</th>
                            <th class="py-3 px-4 text-center">CPF</th>
                            <th class="py-3 px-4 text-center">Ações</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200">
                        <% medicos.forEach((medico) => { %>
                            <tr>
                                <td class="py-3 px-4 text-center"><%= medico.usuario_nome %></td>
                                <td class="py-3 px-4 text-center"><%= medico.especialidade %></td>
                                <td class="py-3 px-4 text-center"><%= medico.crm %></td>
                                <td class="py-3 px-4 text-center"><%= medico.usuario_cpf %></td>
                                <td class="py-3 px-4 flex justify-center gap-4">
                                    <a href="/medicos/editar/<%= medico.medico_id %>" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition">Editar</a>
                                    <form action="/medicos/<%= medico.medico_id %>?_method=DELETE" method="POST">
                                        <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition">Excluir</button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } %>
    </main>

</body>
</html>
